# FEM WebAssembly output

Generated first-party FEM artifacts belong here for distribution. The trusted native Tet4 core and versioned C ABI live under `native/fem`; Task 10 owns the reproducible Emscripten build and checked-in direct-local-compatible runtime artifacts, so build output remains untracked here until that integration is established.
